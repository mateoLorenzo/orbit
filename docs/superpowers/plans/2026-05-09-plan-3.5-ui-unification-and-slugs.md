# Plan 3.5 — UI unification + slug URLs + image opt-out

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Unificar el lesson flow eliminando `lesson-flow-generic/` (parametrizar `module-learning-flow/` para que sirva tanto al demo como a subjects nuevos sin cambio visual del demo). Migrar URLs de UUID a slug. Pausar el fan-out a image-generator hasta que tengamos saldo en FAL/Higgsfield.

**Architecture:** `module-learning-flow/index.tsx` recibe ahora `lessonData` opcional como prop. Si no se pasa, usa los fixtures hardcoded actuales (modo demo). Si se pasa, construye los `Step[]` desde la data real (paragraphs, audio_url, quiz). El `narration` prop con timestamps queda solo para demo (real subjects muestran paragraphs sin highlight progresivo, audio reproducible aparte). URLs: subjects + nodes ganan columna `slug` UNIQUE; lookup por slug; rutas Next.js sin cambio (los `[id]` dynamic segments aceptan cualquier string).

**Tech Stack:** Drizzle ORM (migration) · Next.js 14 app router · TanStack Query

---

## File Structure

**Crear:**
- `lib/slug.ts` — `slugify(name)` + `uniquifySlug(base, existing)` helpers

**Modificar:**
- `lib/db/schema.ts` — `slug text NOT NULL` en subjects + nodes (unique constraint subject vía DB; per-subject vía drizzle index)
- Migration drizzle generada por `pnpm db:generate`
- `lib/db/queries.ts` — `getSubjectBySlug`, `listNodesWithProgress` devuelve `slug`, `createSubject` calcula slug, `nodeBySlug`
- `lib/api/subjects.ts` — `getSubject(slug)` + `NodeWithProgress.slug`
- `lib/hooks/use-subject.ts` — acepta slug en lugar de UUID
- `lib/hooks/use-subjects.ts` — `useCreateSubject` redirige a slug
- `app/subjects/[id]/page.tsx` — rename param a `slug`, lookup por slug
- `app/subjects/[id]/lessons/[nodeId]/page.tsx` — params son slugs, lookup nested
- `app/api/subjects/[id]/route.ts` — GET por slug
- `app/api/subjects/[id]/nodes/route.ts` — devuelve nodes con slug
- `lib/domain/adapters.ts` — `mapSubjectRow` incluye slug
- `components/module-learning-flow/index.tsx` — acepta `lessonData?: GenericLessonData` prop
- `components/module-learning-flow/steps.ts` — exportar `buildStepsFromData(lesson)` además del existente `buildSteps(node)`
- `components/module-learning-flow/types.ts` — agregar `GenericLessonData` interface
- `components/subject-detail-view.tsx` — links navegan con slug
- `lambdas/graph-recalc/index.ts` — comentar el fan-out a image queue (tag `// TODO Plan 4: re-enable when FAL/Higgsfield ready`)

**Borrar:**
- `components/lesson-flow-generic/` (entera, 8 files)

---

## Phase 0 — Image opt-out (5 min)

### Task 1: Comentar fan-out a image queue en graph-recalc

**File:** Modify `lambdas/graph-recalc/index.ts`

- [ ] **Step 1: Comentar el bloque**

Localizar el bloque agregado en P3.5:
```ts
// Same node set fan-out to image generation (parallel pipeline).
for (const nodeId of nodesToGenerate) {
  await sqs.send(
    new SendMessageCommand({
      QueueUrl: Resource.ImageGenerationQueue.url,
      MessageBody: JSON.stringify({ nodeId, subjectId }),
    }),
  )
}
console.log('graph-recalc: fan-out image-gen', { subjectId, count: nodesToGenerate.length })
```

Reemplazar por:
```ts
// TODO Plan 4: re-enable when FAL/Higgsfield billing is ready.
// for (const nodeId of nodesToGenerate) {
//   await sqs.send(
//     new SendMessageCommand({
//       QueueUrl: Resource.ImageGenerationQueue.url,
//       MessageBody: JSON.stringify({ nodeId, subjectId }),
//     }),
//   )
// }
// console.log('graph-recalc: fan-out image-gen', { subjectId, count: nodesToGenerate.length })
```

- [ ] **Step 2: Deploy**

```bash
eval "$(aws configure export-credentials --profile personal --format env)"
pnpm sst deploy --stage dev
```

- [ ] **Step 3: Commit**

```bash
git add lambdas/graph-recalc/index.ts
git commit -m "$(cat <<'EOF'
chore(lambda): pause image fan-out until video provider is ready

FAL balance is exhausted; we'll switch to Higgsfield image-to-video
in Plan 4 once billing is sorted. Lesson + audio fan-outs stay
active so the demo flow keeps working with text + narration.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 1 — Slug migration + queries (45 min)

### Task 2: Slugify helpers

**Files:**
- Create: `lib/slug.ts`
- Test: `lib/__tests__/slug.test.ts`

- [ ] **Step 1: Test failing**

Update `vitest.config.ts` include to add `lib/__tests__/`:
```ts
include: [
  'lib/server/**/*.test.ts',
  'lib/db/**/*.test.ts',
  'lib/api/**/*.test.ts',
  'lib/domain/**/*.test.ts',
  'lib/__tests__/**/*.test.ts',
],
```

Create `lib/__tests__/slug.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { slugify, uniquifySlug } from '../slug'

describe('slugify', () => {
  it('lowercases + dashes', () => {
    expect(slugify('Historia de los Procesos')).toBe('historia-de-los-procesos')
  })
  it('strips accents', () => {
    expect(slugify('La Revolución de Mayo')).toBe('la-revolucion-de-mayo')
  })
  it('strips non-alphanumerics', () => {
    expect(slugify('Node.js & TypeScript!')).toBe('node-js-typescript')
  })
  it('collapses dashes + trims', () => {
    expect(slugify('  --foo--bar--  ')).toBe('foo-bar')
  })
  it('returns "untitled" if input is empty', () => {
    expect(slugify('   ')).toBe('untitled')
  })
})

describe('uniquifySlug', () => {
  it('returns base if not in set', () => {
    expect(uniquifySlug('foo', new Set())).toBe('foo')
  })
  it('appends -2 if base exists', () => {
    expect(uniquifySlug('foo', new Set(['foo']))).toBe('foo-2')
  })
  it('appends next available int', () => {
    expect(uniquifySlug('foo', new Set(['foo', 'foo-2', 'foo-3']))).toBe('foo-4')
  })
})
```

Run: `pnpm test lib/__tests__/slug.test.ts` → FAIL.

- [ ] **Step 2: Implementar**

Create `lib/slug.ts`:
```ts
export function slugify(input: string): string {
  const normalized = input
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, '') // strip accents
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/^-+|-+$/g, '')
  return normalized || 'untitled'
}

export function uniquifySlug(base: string, existing: Set<string>): string {
  if (!existing.has(base)) return base
  let i = 2
  while (existing.has(`${base}-${i}`)) i++
  return `${base}-${i}`
}
```

Run: `pnpm test lib/__tests__/slug.test.ts` → PASS (8 tests).

- [ ] **Step 3: Commit**

```bash
git add lib/slug.ts lib/__tests__/slug.test.ts vitest.config.ts
git commit -m "$(cat <<'EOF'
feat(slug): slugify + uniquifySlug helpers

Used by createSubject and the migration backfill to derive
URL-friendly slugs from human-readable names. Strips accents
(crítico para nombres en español).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: DB migration — add `slug` columns

**Files:**
- Modify: `lib/db/schema.ts`
- Generated: drizzle migration

- [ ] **Step 1: Schema edits**

Modify `lib/db/schema.ts`:

En el bloque `subjects` (~línea 42-56), agregar entre `description` y `lastUploadAt`:
```ts
slug: text('slug').notNull(),
```
Y en el array de indexes/constraints (segundo arg de `pgTable`), agregar:
```ts
unique('subjects_slug_unique').on(t.slug),
```
(Importar `unique` desde drizzle-orm/pg-core si no está; sí está, viene en el import existente.)

En el bloque `nodes` (~línea 124-148), agregar entre `contentBrief` y `topicEmbedding`:
```ts
slug: text('slug').notNull(),
```
Y en el array de constraints, agregar:
```ts
unique('nodes_slug_per_subject_unique').on(t.subjectId, t.slug),
```

- [ ] **Step 2: Generar migration**

```bash
pnpm db:generate
```
Salida: `drizzle/0001_*.sql` (o similar).

- [ ] **Step 3: Backfill manual**

La migration generada va a fallar al aplicar porque `slug` es NOT NULL y las filas existentes no tienen valor. Necesitamos backfillear primero. Crear `scripts/backfill-slugs.ts`:

```ts
import 'dotenv/config'
import { drizzle } from 'drizzle-orm/postgres-js'
import postgres from 'postgres'
import * as schema from '../lib/db/schema'
import { slugify, uniquifySlug } from '../lib/slug'

const url = process.env.DATABASE_URL
if (!url) throw new Error('DATABASE_URL not set')

const client = postgres(url, { prepare: false, max: 1 })
const db = drizzle(client, { schema })

async function main() {
  // Add columns as nullable first via raw SQL (so existing rows don't break)
  await client`ALTER TABLE subjects ADD COLUMN IF NOT EXISTS slug text`
  await client`ALTER TABLE nodes ADD COLUMN IF NOT EXISTS slug text`

  // Backfill subjects
  const subjects = await client<{ id: string; name: string }[]>`SELECT id, name FROM subjects WHERE slug IS NULL`
  const usedSubjectSlugs = new Set<string>(
    (await client<{ slug: string }[]>`SELECT slug FROM subjects WHERE slug IS NOT NULL`).map((r) => r.slug),
  )
  for (const s of subjects) {
    const base = slugify(s.name)
    const slug = uniquifySlug(base, usedSubjectSlugs)
    usedSubjectSlugs.add(slug)
    await client`UPDATE subjects SET slug = ${slug} WHERE id = ${s.id}`
    console.log(`subject ${s.id}: ${slug}`)
  }

  // Backfill nodes (per subject)
  const nodes = await client<{ id: string; subject_id: string; title: string }[]>`SELECT id, subject_id, title FROM nodes WHERE slug IS NULL ORDER BY subject_id, created_at`
  const usedNodeSlugsBySubject = new Map<string, Set<string>>()
  for (const n of nodes) {
    let used = usedNodeSlugsBySubject.get(n.subject_id)
    if (!used) {
      const existing = await client<{ slug: string }[]>`SELECT slug FROM nodes WHERE subject_id = ${n.subject_id} AND slug IS NOT NULL`
      used = new Set(existing.map((r) => r.slug))
      usedNodeSlugsBySubject.set(n.subject_id, used)
    }
    const base = slugify(n.title)
    const slug = uniquifySlug(base, used)
    used.add(slug)
    await client`UPDATE nodes SET slug = ${slug} WHERE id = ${n.id}`
  }
  console.log(`backfilled ${nodes.length} nodes`)

  // Now apply NOT NULL + UNIQUE constraints
  await client`ALTER TABLE subjects ALTER COLUMN slug SET NOT NULL`
  await client`ALTER TABLE subjects ADD CONSTRAINT subjects_slug_unique UNIQUE (slug)`
  await client`ALTER TABLE nodes ALTER COLUMN slug SET NOT NULL`
  await client`ALTER TABLE nodes ADD CONSTRAINT nodes_slug_per_subject_unique UNIQUE (subject_id, slug)`

  await client.end()
}
main().catch((e) => { console.error(e); process.exit(1) })
```

Add to `package.json` scripts:
```json
"backfill:slugs": "tsx --env-file=.env.local scripts/backfill-slugs.ts"
```

- [ ] **Step 4: Run backfill**

```bash
pnpm backfill:slugs
```
Expected: print de cada subject con su slug + count de nodos.

- [ ] **Step 5: Verify columns**

Via supabase MCP o psql:
```sql
SELECT name, slug FROM subjects ORDER BY created_at;
SELECT s.name AS subject, n.title, n.slug FROM nodes n JOIN subjects s ON s.id = n.subject_id ORDER BY s.name, n.created_at LIMIT 20;
```

- [ ] **Step 6: Commit**

```bash
git add lib/db/schema.ts drizzle/ scripts/backfill-slugs.ts package.json
git commit -m "$(cat <<'EOF'
feat(db): add slug columns to subjects + nodes (backfilled)

Subjects.slug is globally unique. Nodes.slug is unique per
subject (composite). Backfill via scripts/backfill-slugs.ts
runs once before constraints are applied so existing rows
don't break the migration.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Queries — slug-aware

**File:** Modify `lib/db/queries.ts`

- [ ] **Step 1: createSubject calcula slug**

Localizar `createSubject`. Reemplazar:
```ts
export async function createSubject(input: { name: string; description?: string | null }) {
  const [row] = await db
    .insert(schema.subjects)
    .values({
      userId: CURRENT_USER_ID,
      name: input.name,
      description: input.description ?? null,
    })
    .returning()
  return row
}
```
por:
```ts
import { slugify, uniquifySlug } from '@/lib/slug'

export async function createSubject(input: { name: string; description?: string | null }) {
  const base = slugify(input.name)
  const existing = await db
    .select({ slug: schema.subjects.slug })
    .from(schema.subjects)
  const slug = uniquifySlug(base, new Set(existing.map((r) => r.slug)))
  const [row] = await db
    .insert(schema.subjects)
    .values({
      userId: CURRENT_USER_ID,
      name: input.name,
      description: input.description ?? null,
      slug,
    })
    .returning()
  return row
}
```

- [ ] **Step 2: getSubjectBySlug**

Agregar (después de `getSubject` existente):
```ts
export async function getSubjectBySlug(slug: string) {
  const [row] = await db
    .select()
    .from(schema.subjects)
    .where(and(eq(schema.subjects.slug, slug), eq(schema.subjects.userId, CURRENT_USER_ID)))
    .limit(1)
  return row ?? null
}
```

- [ ] **Step 3: listNodesWithProgress devuelve slug**

Localizar el `select({ ... })` de `listNodesWithProgress`. Agregar `slug`:
```ts
.select({
  id: schema.nodes.id,
  pathId: schema.nodes.pathId,
  slug: schema.nodes.slug,
  title: schema.nodes.title,
  contentBrief: schema.nodes.contentBrief,
  progressStatus: schema.progress.status,
})
```

- [ ] **Step 4: getNodeBySlug**

Agregar al final del archivo:
```ts
export async function getNodeBySlug(subjectId: string, nodeSlug: string) {
  const [row] = await db
    .select()
    .from(schema.nodes)
    .where(and(eq(schema.nodes.subjectId, subjectId), eq(schema.nodes.slug, nodeSlug)))
    .limit(1)
  return row ?? null
}
```

- [ ] **Step 5: TS + tests**

```bash
pnpm tsc --noEmit
pnpm test
```
Expected: clean. Si tests rompen porque mockean schema, agregar `slug: 'subjects.slug'` al mock object.

- [ ] **Step 6: Commit**

```bash
git add lib/db/queries.ts
git commit -m "$(cat <<'EOF'
feat(queries): slug-aware lookups + createSubject computes slug

createSubject auto-derives a unique slug from the name using
slugify + uniquifySlug. getSubjectBySlug + getNodeBySlug expose
the new lookups. listNodesWithProgress includes slug so the
frontend can build URLs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 2 — API + frontend slug routing (45 min)

### Task 5: API routes aceptan slug

**Files:**
- Modify: `app/api/subjects/[id]/route.ts` (the `[id]` segment is now a slug — Next.js doesn't care about the name)
- Modify: `app/api/subjects/[id]/nodes/route.ts`

- [ ] **Step 1: GET /api/subjects/[id] usa slug**

Modify `app/api/subjects/[id]/route.ts`:
```ts
import { NextResponse } from 'next/server'
import { getSubjectBySlug } from '@/lib/db/queries'

export const dynamic = 'force-dynamic'

export async function GET(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id: slug } = await params
  const subject = await getSubjectBySlug(slug)
  if (!subject) return NextResponse.json({ error: 'not found' }, { status: 404 })
  return NextResponse.json({ subject })
}
```

- [ ] **Step 2: GET /api/subjects/[id]/nodes usa slug → uuid**

Modify `app/api/subjects/[id]/nodes/route.ts`:
```ts
import { NextResponse } from 'next/server'
import { getSubjectBySlug, listNodesWithProgress } from '@/lib/db/queries'

export const dynamic = 'force-dynamic'

export async function GET(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id: slug } = await params
  const subject = await getSubjectBySlug(slug)
  if (!subject) return NextResponse.json({ error: 'not found' }, { status: 404 })
  const nodes = await listNodesWithProgress(subject.id)
  return NextResponse.json({ nodes })
}
```

- [ ] **Step 3: ¿Files routes también?**

`/api/subjects/[id]/files` actualmente usa el mismo getSubject(uuid). Necesitamos resolver slug → uuid antes de usarlo internamente. Modify `app/api/subjects/[id]/files/route.ts`:

Localizar las dos llamadas a `getSubject(id)` (en GET y POST). Reemplazar por:
```ts
const subject = await getSubjectBySlug(id)
```
Y donde se referencia `subjectId` para insertar files o presignar, usar `subject.id` (UUID interno).

Mismo cambio en `app/api/subjects/[id]/files/[fileId]/route.ts`. Aquí `id` es slug, `fileId` sigue siendo UUID:
```ts
const { id: slug, fileId } = await params
const subject = await getSubjectBySlug(slug)
if (!subject) return NextResponse.json({ error: 'not found' }, { status: 404 })
const row = await deleteFile(subject.id, fileId)
```

- [ ] **Step 4: Verify**

```bash
pnpm dev > /tmp/devlog.txt 2>&1 &
DEV_PID=$!
sleep 8
SLUG=$(curl -s http://localhost:3000/api/subjects | jq -r '.subjects[0].slug')
echo "Test slug: $SLUG"
curl -s "http://localhost:3000/api/subjects/$SLUG" | jq '.subject.name'
curl -s "http://localhost:3000/api/subjects/$SLUG/nodes" | jq '.nodes | length'
kill $DEV_PID 2>/dev/null
wait $DEV_PID 2>/dev/null
```
Expected: name string + node count integer.

- [ ] **Step 5: Commit**

```bash
git add 'app/api/subjects/[id]/route.ts' 'app/api/subjects/[id]/nodes/route.ts' 'app/api/subjects/[id]/files/route.ts' 'app/api/subjects/[id]/files/[fileId]/route.ts'
git commit -m "$(cat <<'EOF'
refactor(api): subject routes accept slug instead of UUID

Internal queries still use the UUID — we resolve slug → row →
row.id and pass that to file/node helpers. Next.js dynamic
segment name [id] is preserved for path stability; only the
parsed value's semantics change.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Frontend — hooks + adapter slug-aware

**Files:**
- Modify: `lib/api/subjects.ts`
- Modify: `lib/hooks/use-subject.ts`, `lib/hooks/use-subjects.ts`, `lib/hooks/use-nodes.ts`, `lib/hooks/use-files.ts`
- Modify: `lib/domain/adapters.ts`

- [ ] **Step 1: API helpers usan slug**

Modify `lib/api/subjects.ts`:
```ts
export interface NodeWithProgress {
  id: string
  slug: string
  pathId: string
  title: string
  contentBrief: string
  progressStatus: 'locked' | 'available' | 'in_progress' | 'mastered'
}

export const listSubjects = () => api<{ subjects: DBSubject[] }>('/api/subjects')
export const getSubject = (slug: string) => api<{ subject: DBSubject }>(`/api/subjects/${slug}`)
export const createSubject = (input: { name: string; description?: string }) =>
  api<{ subject: DBSubject }>('/api/subjects', { method: 'POST', body: JSON.stringify(input) })
export const listNodes = (subjectSlug: string) =>
  api<{ nodes: NodeWithProgress[] }>(`/api/subjects/${subjectSlug}/nodes`)
```

- [ ] **Step 2: Files API usa slug**

Modify `lib/api/files.ts`. Renombrar parámetros para claridad:
```ts
export const listFiles = (subjectSlug: string) =>
  api<{ files: FileRow[] }>(`/api/subjects/${subjectSlug}/files`)

export const requestUpload = (
  subjectSlug: string,
  input: { filename: string; mimeType: string; sizeBytes: number },
) => api<...>(`/api/subjects/${subjectSlug}/files`, { method: 'POST', body: JSON.stringify(input) })

export const deleteFile = async (subjectSlug: string, fileId: string) => {
  const res = await fetch(`/api/subjects/${subjectSlug}/files/${fileId}`, { method: 'DELETE' })
  ...
}
```

- [ ] **Step 3: useCreateSubject redirige al slug**

Modify `lib/hooks/use-subjects.ts`. La mutation onSuccess actualmente hace `router.push(\`/subjects/${subject.id}\`)`. Cambiar a:
```ts
router.push(`/subjects/${subject.slug}`)
```

- [ ] **Step 4: mapSubjectRow incluye slug**

Modify `lib/domain/adapters.ts`. Agregar `slug` al UI Subject. Si `lib/types.ts` no tiene `slug` en Subject, agregarlo:
```ts
export interface Subject {
  id: string
  slug: string
  name: string
  ...
}
```

Y en `mapSubjectRow`:
```ts
export function mapSubjectRow(row: DBSubject): UISubject {
  ...
  return {
    id: row.id,
    slug: row.slug,
    name: row.name,
    ...
  }
}
```

- [ ] **Step 5: TS + tests**

```bash
pnpm tsc --noEmit
pnpm test
```

- [ ] **Step 6: Commit**

```bash
git add lib/api lib/hooks lib/domain lib/types.ts
git commit -m "$(cat <<'EOF'
refactor(frontend): hooks + adapters speak slug

createSubject mutation redirects to /subjects/{slug} after
success. listFiles/requestUpload/deleteFile/listNodes accept
subjectSlug. UI Subject type gains slug.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Page routes usan slug

**Files:**
- Modify: `app/subjects/[id]/page.tsx`
- Modify: `app/subjects/[id]/lessons/[nodeId]/page.tsx`
- Modify: `components/subject-detail-view.tsx` (links a lessons usan slug)

- [ ] **Step 1: subject/[id]/page.tsx**

Modify `app/subjects/[id]/page.tsx`. El uso de `useSubject(id)` ahora recibe slug — mismo código, distinto valor.

(Verify: `const id = params.id as string` — ese string es el slug, todo lo que sigue ya funciona.)

- [ ] **Step 2: lessons/[nodeId]/page.tsx**

Modify `app/subjects/[id]/lessons/[nodeId]/page.tsx`. Acepta `slug` + `nodeSlug`. Lookup interno:
- `useSubject(slug)` → devuelve row → row.id
- `useNodes(slug)` → devuelve nodes con field `slug`
- Para el currentNode buscar por `n.slug === nodeSlug` (no por id)
- Pasar `subject.id`, `currentNode.id` a `LessonFlowGeneric` (que usa `useNodeAssets(uuid)` — los assets se buscan por uuid del nodo)

```tsx
const subjectQuery = useSubject(slug)
const subjectRow = subjectQuery.data
const subject = subjectRow ? mapSubjectRow(subjectRow) : null
const isDemo = isDemoSubject(subject)
const nodesQuery = useNodes(slug)

// ...
const realNodes = nodesQuery.data ?? []
const currentNode = realNodes.find((n) => n.slug === nodeSlug)
```

Cambiar TODOS los `router.push(\`/subjects/${subjectId}/lessons/${nextNode.id}\`)` por `router.push(\`/subjects/${slug}/lessons/${nextNode.slug}\`)`.

Donde se pasaba `subjectId` (UUID) a componentes hijos que después usaban `useNodeAssets(nodeId)`, mantener UUID pero el routing UX usa slug.

- [ ] **Step 3: subject-detail-view.tsx links**

Modify `components/subject-detail-view.tsx`. Localizar `onStartLesson`:
```ts
const onStartLesson = (node: ContentNode) => {
  router.push(`/subjects/${subject.slug}/lessons/${node.id}`)
}
```

`node.id` viene del adapter `nodeToContentNode`. Necesita pasar slug también. Modify el adapter:
```ts
function nodeToContentNode(
  n: { id: string; slug: string; title: string; contentBrief: string; progressStatus: ... },
  index: number,
): ContentNode {
  return {
    id: n.slug, // use slug as the routing identifier in UI
    title: n.title,
    description: n.contentBrief,
    type: 'clase',
    status: statusMap[n.progressStatus],
    order: index + 1,
  }
}
```

(O agregar un `slug` field separado a `ContentNode`. Para minimizar cambios, hago `id: n.slug` y el lesson page busca por slug.)

- [ ] **Step 4: TS + Verify**

```bash
pnpm tsc --noEmit
pnpm dev > /tmp/devlog.txt 2>&1 &
DEV_PID=$!
sleep 8
SLUG=$(curl -s http://localhost:3000/api/subjects | jq -r '.subjects[0].slug')
echo "Test slug: $SLUG"
curl -s -o /dev/null -w "HTTP %{http_code}\n" "http://localhost:3000/subjects/$SLUG"
kill $DEV_PID 2>/dev/null
wait $DEV_PID 2>/dev/null
```
Expected: HTTP 200.

- [ ] **Step 5: Commit**

```bash
git add app/subjects components/subject-detail-view.tsx
git commit -m "$(cat <<'EOF'
refactor(routes): page navigation uses slugs end-to-end

Home → subject card → /subjects/{slug}; subject card → lesson
card → /subjects/{slug}/lessons/{nodeSlug}. The internal
useNodeAssets lookup still uses UUID since the assets endpoint
keys by node UUID — slugs only affect the URL bar.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 3 — UI unification: parameterize module-learning-flow (1.5h)

### Task 8: Add `GenericLessonData` type + `buildStepsFromData`

**Files:**
- Modify: `components/module-learning-flow/types.ts`
- Modify: `components/module-learning-flow/steps.ts`

- [ ] **Step 1: Type para data dinámica**

Modify `components/module-learning-flow/types.ts`. Al final, agregar:
```ts
export interface GenericLessonData {
  paragraphs: string[]
  quiz: Array<{
    question: string
    options: string[]
    correctIndex?: number  // present only for demo; real quiz validates server-side
  }>
  videoSrc?: string
  audioSrc?: string
}
```

- [ ] **Step 2: buildStepsFromData**

Modify `components/module-learning-flow/steps.ts`. Mantener `buildSteps(node)` existente intacto. Agregar:
```ts
import type { GenericLessonData, Step } from './types'

export function buildStepsFromData(
  lesson: GenericLessonData,
  fallbackImage = '/learning-landscape.png',
): Step[] {
  const steps: Step[] = [{ kind: 'intro' }]
  // Single content slide with all paragraphs.
  steps.push({
    kind: 'content',
    image: fallbackImage,
    video: lesson.videoSrc,
    paragraphs: lesson.paragraphs,
  })
  // One quiz step per question.
  for (const q of lesson.quiz) {
    steps.push({
      kind: 'quiz',
      question: q.question,
      options: q.options,
      correctIndex: q.correctIndex ?? 0, // server validates; UI uses for demo only
    })
  }
  steps.push({ kind: 'done' })
  return steps
}
```

- [ ] **Step 3: TS check**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add components/module-learning-flow/types.ts components/module-learning-flow/steps.ts
git commit -m "$(cat <<'EOF'
feat(module-flow): add GenericLessonData + buildStepsFromData

Parametrized step builder for non-demo subjects. Demo flow's
existing buildSteps(node) stays untouched. Both produce the
same Step[] shape so the orchestrator handles them identically.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Module-learning-flow acepta `lessonData` prop

**File:** Modify `components/module-learning-flow/index.tsx`

- [ ] **Step 1: Extender props**

Modify `components/module-learning-flow/types.ts`. Localizar `ModuleLearningFlowProps`. Agregar:
```ts
export interface ModuleLearningFlowProps {
  subject: Subject
  node: ContentNode
  onExit: () => void
  onContinueNext?: () => void
  lessonData?: GenericLessonData  // NEW: when provided, overrides the demo's hardcoded steps
  audioSrc?: string                // NEW: when provided, used as the audio src instead of demo narration
}
```

- [ ] **Step 2: Index switches steps source**

Modify `components/module-learning-flow/index.tsx`. Localizar:
```ts
const steps = useMemo(() => buildSteps(node), [node])
```
Reemplazar por:
```ts
import { buildStepsFromData } from './steps'

const steps = useMemo(
  () => (lessonData ? buildStepsFromData(lessonData) : buildSteps(node)),
  [lessonData, node],
)
```

Y donde se determina `lessonNarration` (el bloque que decide hardcoded `FIRST_LESSON_NARRATION`/`SECOND_LESSON_NARRATION`), envolver en `lessonData ? undefined : (...)`:
```ts
const lessonNarration = lessonData
  ? undefined
  : isFirstLessonAnimatedSlide
    ? FIRST_LESSON_NARRATION
    : isSecondLessonAnimatedSlide
      ? SECOND_LESSON_NARRATION
      : undefined
```

(So real subjects don't try to use timestamp-synced narration that doesn't exist.)

- [ ] **Step 3: ContentScreen acepta audioSrc fallback**

Modify `components/module-learning-flow/content-screen.tsx`. Si recibe `narration` undefined pero hay un audio simple (mp3), debe poder reproducirlo. Esta parte requiere lectura del componente actual para entender cómo wirear sin romper el demo.

Read first, then minimal change: hook `useNarrationAudio` ya recibe `narration?: LessonNarration`. Si es undefined, no hace nada. Para audio simple, agregar prop `audioSrc?: string` que renderiza un `<audio>` plano si no hay narration.

(Detalle de implementación: si la complejidad es mucha, el implementer puede simplemente skip audio para subjects nuevos en esta iteración y dejarlo para Plan 4 con el video lambda. Reportar como DONE_WITH_CONCERNS si ese es el caso.)

- [ ] **Step 4: TS check**

```bash
pnpm tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add components/module-learning-flow
git commit -m "$(cat <<'EOF'
feat(module-flow): accept lessonData prop for non-demo subjects

When lessonData is passed, buildStepsFromData replaces the
hardcoded demo steps. lessonNarration is skipped (real audio
has no per-line timestamps). The demo still receives the
default behavior when lessonData is undefined — zero visual
change.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Lesson page usa ModuleLearningFlow para todos

**File:** Modify `app/subjects/[id]/lessons/[nodeId]/page.tsx`

- [ ] **Step 1: Reescribir routing**

Reemplazar el contenido del archivo:
```tsx
'use client'

import { useParams, useRouter } from 'next/navigation'
import Link from 'next/link'
import AppSidebar from '@/components/app-sidebar'
import ModuleLearningFlow from '@/components/module-learning-flow'
import { useSubject } from '@/lib/hooks/use-subject'
import { useNodes } from '@/lib/hooks/use-nodes'
import { useNodeAssets } from '@/lib/hooks/use-assets'
import { mapSubjectRow } from '@/lib/domain/adapters'
import { isDemoSubject, getDemoLessons, getDemoNode } from '@/lib/demo'
import type { ContentNode } from '@/lib/types'

export default function LessonPage() {
  const params = useParams()
  const router = useRouter()
  const subjectSlug = (params.id as string) ?? ''
  const nodeSlug = (params.nodeId as string) ?? ''

  const subjectQuery = useSubject(subjectSlug)
  const subject = subjectQuery.data ? mapSubjectRow(subjectQuery.data) : null
  const isDemo = isDemoSubject(subject)
  const nodesQuery = useNodes(subjectSlug)

  const realNodes = nodesQuery.data ?? []
  const currentRealNode = realNodes.find((n) => n.slug === nodeSlug)

  // Always call useNodeAssets (with empty string disables it via enabled flag)
  const assetsQuery = useNodeAssets(currentRealNode?.id ?? '')

  const onExit = () => router.push(`/subjects/${subjectSlug}`)

  if (subjectQuery.isLoading) {
    return (
      <div className="flex min-h-screen bg-[#f8f8f8] text-black">
        <AppSidebar />
        <main className="flex min-w-0 flex-1 items-center justify-center">
          <p className="text-base text-black/50">Cargando lección...</p>
        </main>
      </div>
    )
  }

  if (!subject) {
    return (
      <div className="flex min-h-screen bg-[#f8f8f8] text-black">
        <AppSidebar />
        <main className="flex min-w-0 flex-1 items-center justify-center">
          <div className="flex flex-col items-center gap-4 text-center">
            <p className="text-2xl font-medium tracking-[-0.5px]">Materia no encontrada</p>
            <Link
              href="/"
              className="inline-flex h-10 items-center rounded-lg bg-black px-3 text-base font-medium tracking-[-0.32px] text-white transition-colors hover:bg-black/90"
            >
              Volver al inicio
            </Link>
          </div>
        </main>
      </div>
    )
  }

  // Demo branch: hardcoded fixtures, no lessonData prop.
  if (isDemo) {
    const demoNode = getDemoNode(nodeSlug)
    if (!demoNode) return null
    const lessons = getDemoLessons()
    const idx = lessons.findIndex((l) => l.id === demoNode.id)
    const next = idx >= 0 ? lessons[idx + 1] : undefined
    return (
      <ModuleLearningFlow
        subject={{ ...subject, content: lessons }}
        node={demoNode}
        onExit={onExit}
        onContinueNext={
          next ? () => router.push(`/subjects/${subjectSlug}/lessons/${next.id}`) : undefined
        }
      />
    )
  }

  // Generic branch: real lesson data via assetsQuery.
  if (!currentRealNode) {
    return (
      <div className="flex min-h-screen bg-[#f8f8f8] text-black">
        <AppSidebar />
        <main className="flex min-w-0 flex-1 items-center justify-center">
          <div className="flex flex-col items-center gap-4 text-center">
            <p className="text-2xl font-medium tracking-[-0.5px]">Lección no encontrada</p>
            <Link
              href={`/subjects/${subjectSlug}`}
              className="inline-flex h-10 items-center rounded-lg bg-black px-3 text-base font-medium tracking-[-0.32px] text-white transition-colors hover:bg-black/90"
            >
              Volver
            </Link>
          </div>
        </main>
      </div>
    )
  }

  const idx = realNodes.findIndex((n) => n.slug === nodeSlug)
  const next = idx >= 0 ? realNodes[idx + 1] : undefined

  // Build a shim ContentNode for the demo's prop signature.
  const nodeShim: ContentNode = {
    id: currentRealNode.id,
    title: currentRealNode.title,
    description: currentRealNode.contentBrief,
    type: 'clase',
    status: 'en-progreso',
    order: idx + 1,
  }

  const lesson = assetsQuery.data?.lesson
  const audio = assetsQuery.data?.audio
  const lessonData = lesson
    ? {
        paragraphs: lesson.paragraphs,
        quiz: lesson.quiz.map((q) => ({ question: q.question, options: q.options })),
        audioSrc: audio?.url,
      }
    : undefined

  if (!lessonData) {
    return (
      <div className="flex h-screen flex-col items-center justify-center gap-3 bg-[#f8f8f8] text-center">
        <div className="size-10 animate-spin rounded-full border-2 border-black/10 border-t-black" />
        <p className="text-base font-medium tracking-[-0.32px] text-black/60">
          Generando lección a partir de tus materiales...
        </p>
      </div>
    )
  }

  return (
    <ModuleLearningFlow
      subject={subject}
      node={nodeShim}
      lessonData={lessonData}
      audioSrc={audio?.url}
      onExit={onExit}
      onContinueNext={
        next ? () => router.push(`/subjects/${subjectSlug}/lessons/${next.slug}`) : undefined
      }
    />
  )
}
```

- [ ] **Step 2: Borrar lesson-flow-generic**

```bash
rm -rf components/lesson-flow-generic
```

- [ ] **Step 3: TS + tests**

```bash
pnpm tsc --noEmit
pnpm test
```

- [ ] **Step 4: Commit**

```bash
git add 'app/subjects/[id]/lessons/[nodeId]/page.tsx'
git rm -r components/lesson-flow-generic
git commit -m "$(cat <<'EOF'
refactor(lesson-page): unify on ModuleLearningFlow, drop generic

Both demo subject and real subjects render through the same
component now. Real subjects pass lessonData (paragraphs +
quiz + audio URL) and ModuleLearningFlow handles them via
buildStepsFromData. The lesson-flow-generic dir is deleted.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Phase 4 — Smoke E2E + push (15 min)

### Task 11: Verify end-to-end + push

- [ ] **Step 1: Verify**

```bash
pnpm test
pnpm tsc --noEmit
```
Both clean.

- [ ] **Step 2: Browser smoke**

```bash
pnpm dev > /tmp/devlog.txt 2>&1 &
sleep 8
```

Browser:
1. Navegá `http://localhost:3000/`
2. Click en alguna materia con nodes generados (ej. la de E2E que tiene 23 lessons ready). URL debe ser `/subjects/{slug}` (no UUID)
3. Click en una lesson card. URL `/subjects/{slug}/lessons/{nodeSlug}`
4. Visualmente igual al demo: intro screen + content screen layout original (no 2-column nuevo)
5. Audio reproducible si está
6. Quiz completable

```bash
kill %1
```

- [ ] **Step 3: Push**

```bash
git push origin plan-3.5-ui-unification-and-slugs
```

- [ ] **Step 4: Reportar**

---

## Out of scope para Plan 3.5

- Higgsfield image-to-video integration → **Plan 4** (cuando la key esté disponible)
- Podcast a 2 voces → **Plan 4**
- Video con ffmpeg → **Plan 4** (probablemente reemplazado por Higgsfield)
- Profile UI + Vercel deploy → **Plan 5**
